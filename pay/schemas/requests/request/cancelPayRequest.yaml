type: object
required:
  - nonce
  - external_id
properties:
  nonce:
    description: "Nonce used in construction of `API-Sign` header"
    type: string
  # external_id
  $ref: "../../../partials/properties/external_id.yaml"
