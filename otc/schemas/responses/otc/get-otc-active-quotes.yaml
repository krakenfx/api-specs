description: "Response schema for retrieving active OTC quotes."
type: "object"
properties:
  result:
    title: "Active quotes"
    type: "array"
    items:
      $ref: '../../../schemas/objects/otc/active-quote.yaml'
  error:
    $ref: '../../../schemas/objects/errors/error.yaml'
required:
  - error
additionalProperties: false
