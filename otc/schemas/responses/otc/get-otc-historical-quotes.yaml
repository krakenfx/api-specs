description: Response schema for retrieving historical OTC quotes.
type: object
properties:
  result:
    title: "Historical quotes"
    type: array
    items:
      $ref: '../../../schemas/objects/otc/historical-quote.yaml'
  error:
    $ref: '../../../schemas/objects/errors/error.yaml'
required:
  - error
additionalProperties: false