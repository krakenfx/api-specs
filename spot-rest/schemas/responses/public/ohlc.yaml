type: object
properties:
  result:
    type: object
    additionalProperties:
      x-additionalPropertiesName: pair
      $ref: '../../../schemas/objects/public/tickData.yaml'
    properties:
      last:
        type: integer
        description: ID to be used as since when polling for new, committed OHLC data
  error:
    $ref: '../../../schemas/objects/errors/error.yaml'
