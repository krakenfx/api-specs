type: object
properties:
  result:
    type: object
    additionalProperties:
      x-additionalPropertiesName: pair
      $ref: '../../../schemas/objects/public/trade.yaml'
    properties:
      last:
        type: string
        description: ID to be used as since when polling for new trade data
  error:
    $ref: '../../../schemas/objects/errors/error.yaml'
