type: object
properties:
  result:
    title: OrderCancelled
    type: object
    properties:
      count:
        description: Number of orders cancelled
        type: integer
        format: int32
      pending:
        description: If true, orders are pending cancellation
        type: boolean
  error:
    type: array
    items:
      $ref: '../../../../schemas/objects/errors/error.yaml'
example:
    $ref: "../../../../examples/responses/trading/orders/cancel.yaml"